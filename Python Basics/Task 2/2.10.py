# Declare a variable
my_variable = 42

# Print the value of the variable
print(f"Value of the variable: {my_variable}")

# Print the memory address of the variable using the id() function
print(f"Memory address of the variable: {id(my_variable)}")
